# Whisper Face 1-65 execution ledger

This ledger turns the product roadmap into a completion audit. A row may move
to **complete** only when its behavior, automated coverage, installer impact,
and live-platform evidence are linked. `active` means a current implementation
wave owns it; `queued` means it remains in scope; `deferred-phone` preserves the
explicit Mac-first sequencing rather than dropping the item.

| # | Deliverable | State | Completion evidence required |
|---:|---|---|---|
| 1 | Signed/notarized Mac app, package, checksums, rollback, safe updater | active | Full Mac setup installs a generic arm64 Swift/AppKit launcher whose exact checkout, revision, and app digest are bound by an external same-user `0600` receipt, leaving the bundle immutable and preserving a packaged signature byte for byte. Source installs build it unsigned; the package path signs the generic app before the DMG only when Apple credentials are supplied. It validates the LaunchAgent, starts only the existing runtime, and requests that process's real GUI over a same-user `0600` Unix socket bound to the launchd PID and revision; the endpoint carries one fixed byte and never starts a second runtime. Both installers fail before downloads or service changes when the source checkout cannot create, privately protect, and remove an unpredictable proof file. A read-only advisor verifies manifests, artifacts, Apple trust, and rollback linkage. An explicit Mac updater now validates a distinct clean prepared sibling checkout, requires linked verified current/candidate release metadata before apply, revalidates before executing only that checkout's setup and verifier, reports setup-owned network/download/private-state effects as possible, and leaves the old checkout intact for rollback. No Apple signing/notarization credentials were exercised here; permission migration, trusted candidate acquisition, and an automatic updater remain outstanding |
| 2 | Permission/model/hotkey/first-dictation onboarding | active | The native Overview now presents Permissions, Hotkey, Models, and Dictate together with live evidence-backed status, recovery guidance, VoiceOver labels, keyboard behavior, and completion acknowledgement; while microphone or Accessibility evidence is incomplete, Overview and Diagnostics expose one accessible action that opens generic System Settings without changing a grant and the visible status refreshes afterward. Hotkey practice completes only after capture is observed. The side-effect-free AppKit gate exercises every transition, while a physical fresh-Mac walkthrough remains |
| 3 | Confidence/Stable Prefix/processing/recovery HUD | complete | Native Overview states consume runtime capture/confidence/prefix/outbox evidence; a count-only Mac menu entry routes directly to Overview without copying, dismissing, retrying, or reading recovery content, and the existing explicit Copy & Dismiss remains the sole recovery action. GUI tests and an AppKit construction/render pass cover the phases |
| 4 | Unified settings for modes, tones, snippets, vocabulary, corrections, privacy, diagnostics | complete | Native panes cover modes, tones, snippets, vocabulary, corrections, on-demand pronunciation-keyword inspection/export/forget, and privacy with validation and atomic persistence. Diagnostics can save a local `0600` support bundle rebuilt from a strict transcript-free allowlist without upload authority; explicit key-view loops, key equivalents, and Settings-to-Diagnostics routing are exercised by the AppKit gate |
| 5 | Result inspector for alternatives, anchors, edits, context, timing | complete | Transcript-free runtime projection and Results UI expose counts, anchors, canonical edit categories, context influence, confidence, and timing with privacy regressions |
| 6 | VoiceOver, keyboard, contrast, reduced motion, localization readiness | complete | VoiceOver metadata, system colors, Reduce Motion, native Tab loops, key equivalents, Apple language fallback, and semantic English catalog ownership cover Overview, Settings, onboarding, Results, Models, Diagnostics, degraded/enum/accessibility copy, validation, operation failures, and verification states with a bounded inventory and native AppKit gate |
| 7 | Clear non-blocking degraded-mode UX | complete | Blocking microphone/permission/service failures route to Action Needed while a failed fallback remains a non-blocking warning; GUI and AudioPool failure tests cover both |
| 8 | Competitor task-based UX evaluation | active | A neutral six-task protocol and offline evaluator strictly separate measured, unavailable, and claimed-only observations without rankings; no cross-product physical run has been recorded |
| 9 | Representative speech/semantic-safety benchmark corpus | complete | `benchmarks/representative_dictation_cases.json` has 19 synthetic cases covering every declared risk dimension; acoustic labels remain explicit physical-recording requirements |
| 10 | Correction Burden, zero-edit, routing, and latency dashboard | complete | Transcript-free `performance_lab.py evaluate` reports p50/p95/p99, burden, zero-edit, routing, coverage, and delivery with strict schema tests |
| 11 | Apple Silicon and Windows hardware matrix | queued | Reproducible runs on representative real hardware |
| 12 | Scheduled model scorecard and reevaluation cadence | complete | Weekly/manual read-only model audit checks immutable revisions and public provenance, uploads transcript-free evidence before failing on drift, and live-checks all three reviewed pins |
| 13 | Non-flaky warm-path latency budgets in CI | complete | Versioned minimum-sample/p95 compiler budget runs on Windows CI; physical latency claims are explicitly excluded |
| 14 | End-to-end component prewarming/cold-start elimination | active | Closed numeric traces and 20 sample-aware cold/warm guardrails cover audio pool, Tiny, final ASR, Ollama, and total readiness; both Whisper snapshots are installer-preloaded and locally memoized, native Parakeet timing survives rolling assembly, and a content-free cleanup circuit now backs repeated four-second transport failures off from 60 to at most 300 seconds while resetting immediately after success. A warm Mac installer rerun retains Ollama only when its newly rendered plist byte-matches, launchd reports it running, and the endpoint is healthy, preserving loaded-model residency without skipping verification. Deterministic compilation keeps 29/30 golden cases on a 0.291 ms corpus-p95 fast path, up from 24/30, leaving only ambiguous embedded-filler cleanup on Qwen. A paired live 30-case gate rejected both proposed Qwen changes: 128 tokens/3.5 seconds preserved outcomes but improved p95 only 6.65% and max 1.74%, while three-shot prompting lost one baseline acceptance. A separate 24-pair synthetic warm-Mac ASR profile rejected `writev`: zero output mismatches, but wall p95/max improved only 3.11%/2.30% and client-overhead p95 worsened. Both labs have no runtime authority, so prompt, budgets, deadlines, and helper framing remain unchanged; controlled cold-cache and steady-state Mac evidence remains |
| 15 | Automatic gain/noise/reverb/VAD/end-of-speech calibration | active | Closed numeric telemetry now feeds an offline fail-closed policy and seven-case synthetic benchmark: bounded gain/noise-gate/VAD/end-silence candidates require eight records and eight seconds of separated evidence, nonfinite/clipping kills, ambiguous silence/noise/quiet/end-silence/near-saturation headroom stays insufficient, reverb remains explicitly unavailable, and no recommendation has runtime authority pending physical device/quality validation |
| 16 | Long/back-to-back/sleep-wake/device-switch/restart stress coverage | active | Deterministic adapter simulation covers 150 operations across five lifecycle scenarios with 50/50 injected faults recovered and zero nondeterminism; MLX/tqdm now uses a thread lock so normal launchd SIGTERM restarts do not strand a multiprocessing semaphore. The Mac runtime listens for native default-input and workspace-wake events, preserves active ordinary and Flight Recorder takes, then closes stale streams and restores only still-enabled inputs. Parakeet startup plus partial pipe writes/reads now have bounded deadlines and response size; timeout, EOF, malformed, or oversized frames terminate the child, preserve Turbo fallback, and restart lazily. Physical device-switch, sleep/wake, thermal, memory, and long-audio evidence remains |
| 17 | Explainable Last Recognition disagreements and rejections | complete | Transcript-free route, protected-category counts, uncertainty, re-listen status, and fixed skip reasons render in the menu and Results; the exact review route labels the menu root, shows a visible accessible Results advisory, and the submenu now opens that inspector directly without adding data or action authority |
| 18 | Selective Re-listen of uncertain spans | active | Native-timing-only selector merges action/payload spans, caps two 2.4-second microspans, rejects full utterances, and maps validated rolling/speculative word times through exact capture-sample bounds while preserving silence gaps. A validated process-isolated verifier now receives only the selected slices under one absolute deadline; missing audio, cold state, malformed results, timeouts, same-engine evidence, and unsafe contracts fail closed to review. Runtime remains off until the MacBook produces an approved item-19 receipt and the user opts in |
| 19 | Specialized ASR/error verifier comparison | active | The pinned local Whisper Tiny adapter composes with the reusable killable supervisor and exposes nonblocking prewarm readiness. The offline transcript-free activation benchmark compares disposable, prewarmed, and direct whole-span local Tiny on identical private 16 kHz WAV cases with closed accuracy/latency/refusal aggregates and deterministic engine-order rotation. It can write a private `0600` activation receipt only for at least 40 balanced real recordings, zero synthetic cases, ≥95% exact decisions, ≤650 ms p95, zero refusals, and explicit manual review. No physical corpus was run here, so no receipt or activation claim exists yet |
| 20 | Consequence-aware risk routing | active | Eleven protected categories, punctuation/sign-safe disagreement detection, and bounded number-plus-unit claims detect meaning changes in spelled, abbreviated, compact, spoken-decimal, and half/quarter-fraction forms only when paired with an approved measurement unit. Fixed decision receipts, a 16-case exact-category selector benchmark, and a worst-case-per-case 5 ms gate exist. When item-19 evidence is approved and the user opts in, independent confirmations can route uncertain high-risk spans to verified while contradictions, inconclusive results, and failures remain review; physical routing-quality evidence remains |
| 21 | Acoustic Time Machine microspan replay | active | The default-off Mac runtime stores only consequence-plan-selected native-timed microspans after insertion completes, replaces them on the next usable result, and provides in-memory Results Play/Clear controls plus immediate manual/disable clearing; every retained span also expires after 60 monotonic seconds through a locked wipe that stops playback and releases timer, sound, handles, and samples, while physical privacy/usability validation remains |
| 22 | Inspectable personal acoustic keyword memory | active | Exact verified corrections add idempotent global evidence, and the on-demand inspector supports token-free export plus confirmed forget; a strict offline evaluator now compares closed categorical unbiased/biased candidate evidence, emits aggregate keep/kill/insufficient receipts, and refuses synthetic evidence for keep, while the deterministic fixture benchmark records zero physical evidence and no runtime recognition authority exists |
| 23 | Counterfactual Context Firewall | complete | Every finalized insertion-bound compile is compared with a Context Candidate/Personal Prior-free shadow baseline; protected influence is classified without changing recognition, cleanup, insertion, or routing, with adversarial compiler and golden-corpus coverage |
| 24 | Context influence receipts and shadow personalization/model evaluation | active | Results now renders fixed transcript-free no-effect, promotion-candidate, or quarantine receipts with bounded counts, locale fallback, VoiceOver metadata, and privacy tests; any behavior-changing promotion workflow remains outstanding |
| 25 | Delayed cleanup merges only untouched VoiceIR spans | active | The conservative three-way merge now sits behind an idempotent, content-free transaction adapter that requires exact capture/recheck identity, focus, revision, and text plus an injected atomic compare-and-swap. A standalone Mac read-only adapter captures one trusted focused/enabled text destination with bounded transient text/selection, keyed memory-only identity/revision tokens, private-text-redacted representations, and content-free receipts; it has no write callback, AX action, clipboard, persistence, log, runtime, or GUI surface, so an atomic platform writer and runtime evidence remain |
| 26 | Public 50-app insertion/capability corpus | active | Versioned synthetic capability profiles run reproducibly with `uv run benchmark_insertion_reliability.py`; zero real apps have been exercised, so the public 50-app matrix remains outstanding |
| 27 | Adversarial focus/duplicate/typing/relaunch/clipboard/delay tests | active | Deterministic cases cover focus and selection drift, concurrent nearby typing, duplicate callbacks, relaunch-like identity change, unavailable target/clipboard/readback, and phase-delayed readback; manual real-app evidence remains |
| 28 | Four-nines exactly-once insertion measurement | active | The simulation harness repeats every case and reports at-most-once paste-attempt and terminal-receipt violations; it explicitly makes no four-nines or physical reliability claim |
| 29 | Privacy-safe compatibility fingerprint network | active | A closed text-free capability/outcome schema, deterministic coarse token, bounded local minimum-count aggregation, strict private-field rejection, and explicit opt-in export exist; no runtime collection, approved transport, population-anonymity claim, or network service exists yet |
| 30 | Voice Input Protocol and initial adapters | active | The strict v1 contract and canonical bounded JSON codec now have a POSIX local Unix-socket test transport with 0600 endpoint, one request/response frame, deadlines, supported peer-UID enforcement, replacement-safe cleanup, and closed failures; it remains unwired and is not a public SDK/ABI, XPC endpoint, Windows transport, or physical app adapter |
| 31 | Networkless sandboxed XPC speech worker | active | A one-shot, unwired macOS worker now uses a deny-default `sandbox-exec` profile, private bounded Unix IPC, static content-free failures, and live proofs that IPv4 bind and outbound connection are denied; it accepts only non-transcript capture proposals and is not yet XPC, a speech worker, or runtime-active |
| 32 | Provider-neutral model wallet | active | The strict in-process policy for current immutable Parakeet, two Whisper, and Qwen pins exposes a public non-executing eligible-profile order. Warmup now caches one bounded, read-only exact-pin receipt for all four providers and separately records process-local warm-path observations; the Models pane validates and renders both without paths, revisions, downloads, provider imports, sockets, or status-time execution. Filesystem evidence remains capped at `resolved`, historical warm paths never become `ready`, capability attestation stays false, routing remains fail-closed/no-attempt, and Voice Compiler behavior is unchanged |
| 33 | Personal Regression cardinality-boundary tests | complete | `tests/test_personal_regression.py` covers deterministic `MAX_CASES + 1`, `MAX_MAPPINGS + 1`, and `MAX_QUARANTINED + 1` eviction |
| 34 | Full installed-Windows verification | queued | Automated `setup.ps1 --verify` now fails closed unless a strict private launcher receipt, current checkout and locked `dictate.py` command, current-user task principal, and sole expected PowerShell action agree before health/model checks. This proves task-to-launcher configuration but not independently managed Ollama listener ownership; a real Windows verify and first dictation remain |
| 35 | Point-and-Speak | active | Diagnostics keeps its read-only Mac preview, then exposes **Press once** only for a resolved button, checkbox, radio button, tab, menu item, or link after a separate explicit confirmation; text fields and every unlisted role remain inert. The action performs a fresh bounded capture/resolution, requires exact or normalized name evidence with very-high confidence and a wide margin, consumes one session-issued nonce, and rechecks the exact app, focused window, target window, element projection, role, trust, and two-second lease immediately before one `AXPress`; drift, replay, eviction, expiry, weak evidence, unsupported roles, and callback failure do nothing. Phrases, names, target IDs, and native identities remain transient and terminal receipts are content-free. Synthetic resolver coverage remains 17/17; physical-app safety/accuracy remains outstanding |
| 36 | Drop-to-Target | active | A closed non-writing resolver gates source kind, effect, target capability, confidence, margin, and contradictions; its 11-case synthetic corpus has 11 correct outcomes and zero wrong-target resolutions. Mac Diagnostics now exposes the read-only AX adapter only through an explicit transient preview whose user declares a hypothetical role, source kind, and effect; bounded role/name/geometry/state plus `AXDropEnabled` evidence produces a content-free `execution: none` receipt and, only when resolved, one transient accessible name. It cannot prove declared semantics, expose a payload or target ID, persist or log target text, execute a drag/drop/AX action, or make physical keep/kill or accuracy claims |
| 37 | Voice Objects projected across destinations | active | A default-off Mac setting routes only three exact capture-mode command forms into task, email, or calendar projections before cleanup/paste/logging; every parser miss or queue failure follows the existing paste path. After Reveal and a second explicit confirmation, queued task/calendar projections can make one main-thread clipboard copy and a queued email can make one in-process `NSSharingServiceNameComposeEmail` request. A separate clear action keeps only the owned pasteboard change count and does nothing after another clipboard change; macOS exposes no atomic compare-and-clear, so a microscopic external race remains. The always-available menu entry adds only a bounded queued count and opens the existing metadata-first inspector. No path sends, schedules, pastes, auto-dispatches, launches through URLs/process args, changes queue state, or produces payload-bearing receipts |
| 38 | Agent Voice Inbox | active | The strict local queue accepts opt-in drafts idempotently with opaque provenance and atomic private persistence; a direct Mac menu entry routes Settings to Privacy and opens the inspector while exposing only the bounded queued count. The inspector lists content-free metadata, reveals only a selected draft, and offers acknowledge, cancel, terminal purge, separately confirmed email compose, task/calendar copy, and owned-copy clear. Runtime freshly rereads and revalidates the exact queued item, consumes one-shot session capabilities, keeps it queued, never reads clipboard content, and reports only closed content-free states plus attempted; no local-agent dispatch, paste, schedule, or send authority exists |
| 39 | Voice-plus-click confirmation for risky agent actions | active | The Mac runtime and native Privacy pane now wrap the pure content-free gate in an explicit inert four-class ceremony: exact capture-mode voice confirmation is consumed before every text sink, then a distinct enabled click must arrive within 30 monotonic seconds; early clicks, cancellation, expiry, duplicate IDs, and terminal replay fail closed, while action payloads, persistence, clipboard/AX/app writes, and execution callbacks remain absent |
| 40 | Teach-by-demonstration workflows | active | The Mac Privacy pane now explicitly authors inert Finder/Mail/Notes/menu recipes with runtime-generated opaque IDs, metadata-only listing, selected private Reveal/Edit, domain-valid bounded step recording, inert approval, atomic cancel/rollback for recording drafts, and confirmed deletion for approved recipes in protected local state; observation-based capture, replay, app automation, and execution remain absent |
| 41 | Risk sonification/haptic feedback | active | A successful ordinary Mac dictation uses an asynchronous Ping when its consequence receipt routes to review; this is advisory only, while other success remains Pop and insertion failure/outbox remains Funk. Usability and error-rate evidence are still outstanding |
| 42 | Privacy promise, threat model, security policy, disclosure route | complete | `PRIVACY.md`, `SECURITY.md`, and the threat model document current local/LAN/release boundaries; ordinary desktop port 8787 is loopback-only while explicit `--server-only` retains the trusted-LAN boundary. Routine logs replace transcript, correction, learned-term, snippet, focused-app, and phone-preview values with fixed labels and bounded counts; both installers provision all service logs before first service start, repair protection on rerun, and verify current-user-only posture. GitHub private vulnerability reporting is enabled and tested live |
| 43 | Trademark clearance and optional patent/prior-art decision | queued-external | Owner/counsel decision recorded without claiming legal advice |
| 44 | Public reproducible benchmarks and model scorecard | active | `public_scorecard.py` deterministically aggregates five checked-in synthetic suites into a transcript-free 66/66 report with zero critical failures and explicit zero physical apps/audio/model runs; a regression guard keeps this published total equal to generated evidence. Physical results remain outstanding |
| 45 | Architecture/interoperability/SDK/contributor/upgrade/rollback docs | active | Current architecture boundaries, the exact in-process Voice Input Protocol contract, contributor verification/parity rules, and a tested cross-platform update/rollback guide are published. The Mac guide now includes a tested explicit side-by-side plan/apply helper that never fetches a candidate, resets, overwrites source, or removes the rollback checkout, and that requires linked verified current/candidate release metadata before executing the candidate installer; a stable cross-process SDK/public ABI, automatic updates, trusted candidate acquisition, and physical rollback drills remain outstanding |
| 46 | Reproducible demonstration corpora | complete | Public synthetic corpus, deterministic compiler stress replay, privacy-safe observation schema, CLI instructions, and CI regressions are committed |
| 47 | Honest competitor comparison pages | active | The dated official-source comparison documents setup, privacy, pricing, and unknowns for Whisper Face, Wispr Flow, Superwhisper, MacWhisper, OpenWhispr, and Handy; it explicitly publishes no winner until reproducible physical task results exist |
| 48 | Independently assignable GitHub implementation issues | complete | Vertical-slice issues [#6](https://github.com/Aiml3ss/whispering-parrot/issues/6) through [#19](https://github.com/Aiml3ss/whispering-parrot/issues/19) link the roadmap to acceptance criteria and dependencies |
| 49 | Sponsors and voluntary Supporter experiment | active | The public free-local covenant prohibits core feature locks and defines recognition-only proposed Supporter benefits; a nonbinding GitHub interest form is live, but no payment account, final benefits, pricing, or paid conversion exists |
| 50 | Concierge setup/migration offering | active | A bounded one-Mac interest pilot documents scope, private-data exclusions, user-present fulfillment, capacity acceptance, and public-to-private coordination boundaries; no booking, payment, operator capacity, or completed session exists |
| 51 | Pricing/willingness-to-pay experiment | active | The public form measures deduplicated stated support/concierge shape only under dated zero-inclusive reporting and explicitly rejects revenue, conversion, or willingness-to-pay claims; owner-approved price points and observed submissions remain outstanding |
| 52 | Optional E2EE preference/vocabulary/correction/recovery sync | queued | Threat model, protocol, clients, recovery, delete/export |
| 53 | Away-from-home encrypted relay to owned Mac | queued | Ciphertext-only fallback relay and direct-path preference |
| 54 | Team vocabulary/deployment/policy/diagnostics/roles/updates | queued | Self-hosted vertical slice with transcript-free metrics |
| 55 | Enterprise SSO/SCIM/compliance foundations | queued | Demand gate followed by auditable controls and documents |
| 56 | OEM, funded-open-work, Accuracy Bond, certification experiments | queued | One measured pilot before scaling each model |
| 57 | iPhone physical-device capture/keyboard feasibility | deferred-phone | Host/keyboard prototype on supported physical devices |
| 58 | Standalone offline Whisper Face Pocket | deferred-phone | Account-free offline first dictation and recovery |
| 59 | Stable-Prefix voice keyboard with exactly-once insertion | deferred-phone | Marked-text keyboard and lifecycle fault tests |
| 60 | Mobile Voice Compiler and adaptive acoustic front end | deferred-phone | On-device compiler, routes, energy/thermal benchmarks |
| 61 | Durable mobile Dictation Ledger and Voice Outbox | deferred-phone | Crash-safe proposals/commit/acknowledgement fault tests |
| 62 | Encrypted one-scan Mac pairing/Nearby Compute Auction | deferred-phone | Secure pairing, selective cooperation, automatic fallback |
| 63 | Transcript-free correction sync and expiring Context Capsules | deferred-phone | E2EE compact state, provenance, expiry, export/forget |
| 64 | iPhone lifecycle/thermal/battery/accessibility/App Review torture suite | deferred-phone | Physical-device matrix and review/privacy preflight |
| 65 | iPhone control mesh, Watch, AirPods, Siri, Shortcut experiments | deferred-phone | One state machine and measured keep/kill decisions |

## Release-wave rule

Each wave must update this ledger and the GitHub roadmap, audit both installers,
run the repository release gates, report live-platform limitations honestly,
and merge through protected `main`. External credentials or physical hardware
may delay final evidence, but do not remove the deliverable from scope.
